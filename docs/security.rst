Security
============

.. glossary::
   
   key
   	A series of 32 binary bytes.  Each user (and each server) have a pair of keys (one public, one private).  Keys can also be represented as `Y64 <http://www.yuiblog.com/blog/2010/07/06/in-the-yui-3-gallery-base64-and-y64-encoding/>`_ strings.